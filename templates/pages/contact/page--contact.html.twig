<div class="container mx-auto mb-20">
  <main role="main">
    <a id="main-content" tabindex="-1"></a>{# link is in html.html.twig #}

    <div id="page-title" class="mb-4 pb-4 border-b">
      {# {{ page.content.idcui_page_title}} #}
      <h1 class="text-3xl">Website Feedback</h1>
    </div>

    <div class="grid grid-cols-3 xl:grid-cols-7">
      <div class="col-span-3 flex">
        <div class="mx-auto">
          <h2 class="font-bold text-2xl text-secondary-5">Let us know how we can help!</h2>
          <p class="pb-4">
            We would like to hear about your experience using our digital collections
            and would be delighted to answer any questions you might have.
          </p>

          {# Contact page generated Drupal form will be placed here #}
          {{ page.content.idcui_content }}
        </div>
      </div>

      <div class=""></div>

      <div class="col-span-3 flex flex-col">
        <div class="mx-auto my-6 py-4 px-8 border-b border-accent-10 font-bold text-xl text-gray-600">
          <div>The Sheridan Libraries</div>
          <div>Johns Hopkins University</div>
          <div>410-516-8335 (Library Info Desk)</div>
        </div>
        {# TODO: Remove the static sizing style here, when the image is ready #}
        <div class="mx-auto border" style="width: 500px; height: 400px;">
          <img
            src=""
            width=500
            height=400
            alt="Placeholder showing some JHU building that looks nice"
          >
        </div>
      </div>
    </div>
  </main>

</div>
