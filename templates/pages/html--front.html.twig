{#
/**
 * @file
 * Theme override for the basic structure of a single Drupal page.
 *
 * Variables:
 * - logged_in: A flag indicating if user is logged in.
 * - root_path: The root path of the current page (e.g., node, admin, user).
 * - node_type: The content type for the current node, if the page is a node.
 * - head_title: List of text elements that make up the head_title variable.
 *   May contain one or more of the following:
 *   - title: The title of the page.
 *   - name: The name of the site.
 *   - slogan: The slogan of the site.
 * - page_top: Initial rendered markup. This should be printed before 'page'.
 * - page: The rendered page markup.
 * - page_bottom: Closing rendered markup. This variable should be printed after
 *   'page'.
 * - db_offline: A flag indicating if the database is offline.
 * - placeholder_token: The token for generating head, css, js and js-bottom
 *   placeholders.
 *
 * @see template_preprocess_html()
 */
#}
{%
  set body_classes = [
    logged_in ? 'user-logged-in',
    not root_path ? 'path-frontpage' : 'path-' ~ root_path|clean_class,
    node_type ? 'page-node-type-' ~ node_type|clean_class,
    db_offline ? 'db-offline',
  ]
%}
<!DOCTYPE html>
<html{{ html_attributes }}>
  <head>
    <head-placeholder token="{{ placeholder_token }}">
    <title>{{ head_title|safe_join(' | ') }}</title>
    <css-placeholder token="{{ placeholder_token }}">
    <js-placeholder token="{{ placeholder_token }}">
  </head>
  <body{{ attributes.addClass(body_classes) }}>
    {#
      Keyboard navigation/accessibility link to main content section in
      page.html.twig.
    #}
    <a href="#main-content" class="visually-hidden focusable skip-link">
      {{ 'Skip to main content'|t }}
    </a>
    {{ page_top }}

    <div class="w-full bg-blue-spirit relative pb-10">
      <div class="container mx-auto relative z-10">
        <div class="py-4"> {# Contains header(s) #}
          <h1 class="text-4xl font-bold text-blue-heritage">
            DIGITAL REPOSITORIES&nbsp;
            <span class="text-secondary-5">@ JHU</span>
          </h1>
          {# 2xl smaller than above header looks OK #}
          <h2 class="text-2xl font-semibold text-white">
            Collections and scholarship from Johns Hopkins University
          </h2>
        </div>
        {# Featured Items grid #}
        <div class="grid grid-cols-2 gap-12">
          <div class="border"> {#  Card container #}
            <div class=""> {#  Card image container #}
              <img alt="Library Collections image" width=485 height=326>
            </div>
            <div class="px-8 py-5 shadow-md"> {# Card body container #}
              <h3 class="text-2xl font-bold">
                Library Collections
              </h3>
              <p>
                Search digitized historic photographs, advertisements, texts, 
                and more from JHU's unique library collections
              </p>
            </div>
          </div>
          <div class="border"> {#  Card container #}
            <div class=""> {#  Card image container #}
              <img alt="JHU Scholarship image" width=485 height=326>
            </div>
            <div class="px-8 py-5 shadow-md"> {# Card body container #}
              <h3 class="text-2xl font-bold">
                JHU Scholarship
              </h3>
              <p>
                Find publications, theses, and dissertations by JHU faculty 
                and students
              </p>
            </div>
          </div>
        </div>
      </div>
      <div class="bg-gray-200 absolute bottom-0 w-full h-40 z-0"></div>
    </div>

    {# TODO: These need to flow better for smaller screen sizes #}
    <div class="w-full bg-white py-10">
      <div class="container mx-auto">
        <div class="flex flex-row">
          <div class="flex-grow">
            <p class="mb-6">
              The five Sheridan Libraries - the Milton S. Eisenhower Library, the Brody 
              Learning Commons, the Albert D. Hutzler Reading Room, the George Peabody 
              Library, and the John Work Garrett Library - provide the major research 
              library resources for Johns Hopkins University.
            </p>
            <p>
              Together, the Libraries contain more than 4.2 million volumes and provide 
              around-the-clock access to a rich collection of electronic resources, including 
              more than 154,000 print and e-journals, and more than 1.6 million e-books.
            </p>
          </div>
          <div class="flex-none mx-20">
            <div class="border shadow text-blue-heritage ">
              <h4 class="font-bold px-8 py-4">Need more information?</h4>
              <div class="bg-gray-400 flex justify-center p-6">
                <button class="bg-blue-spirit w-full p-2">Contact Us</button>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    {{ page }}

    {{ page_bottom }}
    <js-bottom-placeholder token="{{ placeholder_token }}">
  </body>
</html>
